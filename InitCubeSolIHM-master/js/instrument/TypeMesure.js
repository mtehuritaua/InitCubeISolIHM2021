class TypeMesure {

    constructor(nom, unite, valMin, valMax) {
        this.nom = nom;
        this.unite = unite;
        this.valMax = valMax;
        this.valMin = valMin;
        console.log("Création d'un TypeMesure nommé " + this.nom);
    }
}